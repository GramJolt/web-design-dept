# CLAUDE.md

このリポジトリは「webデザイン部」— AIエージェント主体で運営される会社の一部署 — の作業リポジトリです。AIエージェント（Claude Codeなど）が主担当としてタスクを実行し、人間（会社オーナー）はIssueでの指示とPRレビューを行います。

## この部署のミッション

Webサイト・LPのデザイン制作・実装をAIエージェント主導で進める。

## 運用ルール

- 作業単位は必ず GitHub Issue に対応させる。Issueなしでの大きな変更は行わない。
- 1 Issue = 1 タスク。大きすぎる依頼は着手前にサブタスクへ分割し、Issueを分ける。
- 作業が終わったら PR を作成し、`.github/PULL_REQUEST_TEMPLATE.md` の項目を埋める。
- 使用技術スタック・デプロイ先の選定など後戻りしにくい決定は着手前にIssue上で確認を取ってから進める。
- 意思決定や手順（SOP）で再利用価値があるものは、コードではなくObsidian Vault（別管理）に記録する旨をPR/Issueコメントで示す。このリポジトリにはドキュメントを増やしすぎない。

## タスクごとのエージェント

`.claude/agents/` にタスク別のサブエージェントを定義済み。役割が該当する場合は該当エージェントに委任する。

| エージェント | 名前 | 担当 | 権限 |
|---|---|---|---|
| `market-researcher` | 湊（みなと） | 市場調査（競合サイト・デザイントレンド調査） | Web調査のみ、コード変更不可 |
| `designer` | 蒼（あおい） | 設計方針（情報設計・ページ構成・デザイン方針） | 設計文書作成のみ、コード変更不可 |
| `implementer` | 磨（みがく） | 実装 | コード編集可、破壊的変更は要確認 |
| `reviewer` | 澄（すみ） | レビュー | 読み取りのみ、コード変更不可 |

想定フロー: 市場調査 → 設計 → 実装 → レビュー（すべてのタスクで全工程を経る必要はなく、タスクの粒度に応じて省略してよい）。

各エージェントの役割定義・権限の詳細はObsidian Vault側（`03_webデザイン部/エージェント定義/`）にも記録している。

## ディレクトリ構成

未定（最初の案件の技術スタックに応じて決める）。

## コーディング規約

- 既存のコードスタイルに合わせる。
- コメントは自明でない意図がある場合のみ。
- 依頼された範囲を超えたリファクタリングや抽象化は行わない。

## 表示確認時の注意（プライバシー）

- ブラウザでの表示確認が必要な場合、実機ブラウザの画面全体をキャプチャする手法（`screencapture`等）は使わない。無関係な他プロジェクト・他ウィンドウの内容が写り込むリスクがある。
- 可能な範囲でヘッドレスブラウザやHTML/CSSの静的検証（タグ対応チェック等）で代替する。目視確認がどうしても必要な場合は、その旨を報告してオーナーに委ねる。
